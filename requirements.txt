# This server uses the v1 FastMCP API; v2 requires a separate migration.
mcp>=1.0.0,<2
httpx>=0.27.0
numpy>=1.26.0
pytest>=8.0.0
# Optional: pip install sentence-transformers>=4.0.0  (only needed if reranker.enabled = true)
